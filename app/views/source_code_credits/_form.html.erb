<%= form_with(model: source_code_credit, local: true) do |form| %>
  <% if source_code_credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(source_code_credit.errors.count, "error") %> prohibited this source_code_credit from being saved:</h2>

      <ul>
      <% source_code_credit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name_of_work %>
    <%= form.text_field :name_of_work, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :url %>
    <%= form.text_area :url, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :license_kind %>
    <%= form.text_field :license_kind, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :license_text %>
    <%= form.text_area :license_text, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if source_code_credit.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', source_code_credit, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if source_code_credit.persisted? %>
      <%= link_to "Cancel", source_code_credit, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", source_code_credits_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
