<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/backstage/head' %>
  </head>

<body class="<%= controller_name %> <%= action_name %>">    <%= render 'shared/navbar' %>
    <%= render 'shared/backstage/notices' %>

    <div class="container">
      <%= yield %>
    </div>

    <%= render 'shared/backstage/footer' %>
  </body>
</html>
